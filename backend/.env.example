# Server
PORT=3000
API_TOKEN=           # optional bearer token for all /api/*
DEFAULT_CLIENT_ID=   # UUID of your tenant (seed once, paste here)

# LLMs (optional)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Supabase (required)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_KEY=  # service role key (server only)